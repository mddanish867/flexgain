import { CoachClient } from "@/components/dashboard/CoachClient";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "AI Coach · FlexGain",
};

export default function CoachPage() {
  return <CoachClient />;
}
